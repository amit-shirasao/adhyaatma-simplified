import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RenukaAshtak1Page } from './renuka-ashtak-1.page';

describe('RenukaAshtak1Page', () => {
  let component: RenukaAshtak1Page;
  let fixture: ComponentFixture<RenukaAshtak1Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RenukaAshtak1Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
