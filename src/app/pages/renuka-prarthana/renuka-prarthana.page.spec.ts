import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RenukaPrarthanaPage } from './renuka-prarthana.page';

describe('RenukaPrarthanaPage', () => {
  let component: RenukaPrarthanaPage;
  let fixture: ComponentFixture<RenukaPrarthanaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RenukaPrarthanaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
