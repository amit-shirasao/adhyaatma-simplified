import { ComponentFixture, TestBed } from '@angular/core/testing';
import { GanapatiAartiPage } from './ganapati-aarti.page';

describe('GanapatiAartiPage', () => {
  let component: GanapatiAartiPage;
  let fixture: ComponentFixture<GanapatiAartiPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(GanapatiAartiPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
