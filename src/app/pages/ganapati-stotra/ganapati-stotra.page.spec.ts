import { ComponentFixture, TestBed } from '@angular/core/testing';
import { GanapatiStotraPage } from './ganapati-stotra.page';

describe('GanapatiStotraPage', () => {
  let component: GanapatiStotraPage;
  let fixture: ComponentFixture<GanapatiStotraPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(GanapatiStotraPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
