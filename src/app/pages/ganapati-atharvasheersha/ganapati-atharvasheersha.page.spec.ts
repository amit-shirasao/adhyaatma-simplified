import { ComponentFixture, TestBed } from '@angular/core/testing';
import { GanapatiAtharvasheershaPage } from './ganapati-atharvasheersha.page';

describe('GanapatiAtharvasheershaPage', () => {
  let component: GanapatiAtharvasheershaPage;
  let fixture: ComponentFixture<GanapatiAtharvasheershaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(GanapatiAtharvasheershaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
