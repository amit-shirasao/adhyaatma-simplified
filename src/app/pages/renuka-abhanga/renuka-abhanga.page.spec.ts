import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RenukaAbhangaPage } from './renuka-abhanga.page';

describe('RenukaAbhangaPage', () => {
  let component: RenukaAbhangaPage;
  let fixture: ComponentFixture<RenukaAbhangaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RenukaAbhangaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
