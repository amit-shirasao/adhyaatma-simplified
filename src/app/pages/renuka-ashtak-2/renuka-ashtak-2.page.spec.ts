import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RenukaAshtak2Page } from './renuka-ashtak-2.page';

describe('RenukaAshtak2Page', () => {
  let component: RenukaAshtak2Page;
  let fixture: ComponentFixture<RenukaAshtak2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RenukaAshtak2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
